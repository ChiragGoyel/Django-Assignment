Steps to start the project.
cd venv/Scripts - activate
install the requirements mentioned in the requirement.txt file inside the library folder
command - pip install -r /path/to/requirements.txt
cd inside the library folder 
command - python manage.py runserver

Admin - username - admin
	password - pass@123

Librarian - username - librarian
	    password - pass@1998

Student - username - student
	  password - pass@1998

We can add multiple users as Librarian and student and they both will have different permissions to access different page.






